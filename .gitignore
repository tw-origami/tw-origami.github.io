.DS_Store

# local Claude Code tooling config (dev-server + local permissions)
.claude/

# large source image pack (only slim foodimg/ is used)
nutrition/food_emoji_pack_high_res_300/

# Note: plan/ used to be excluded here (it has the boys' first names, grades, and
# lesson progress, and this repo is public). 2026-09-05: published on purpose —
# robots.txt + noindex meta tags on those pages keep it out of search/AI-crawler
# indexing, but the pages themselves are public to anyone with the link.
